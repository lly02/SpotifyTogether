{
  "name": "spotifytogether",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon ",
    "build-copy-src": "babel src -d dist/src --presets @babel/preset-typescript --extensions .ts --minified -D --source-maps inline",
    "build-copy-public": "babel public -d dist/public -D --minified -w --source-maps inline",
    "build-copy-root": "babel manifest.json -d dist -D --minified -w --source-maps inline",
    "build": "concurrently --kill-others \"npm:build-*\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.23.0",
    "@babel/core": "^7.23.0",
    "@babel/plugin-transform-typescript": "^7.22.15",
    "@babel/preset-env": "^7.22.20",
    "@babel/preset-typescript": "^7.23.0",
    "concurrently": "^8.2.1",
    "typescript": "^5.2.2"
  }
}
